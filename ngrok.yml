version: "2"
# Get your authtoken from: https://dashboard.ngrok.com/get-started/your-authtoken
authtoken: 33fBxT6iYSCb7gKJIw6LI1Bx7Hy_78pXkbivaHNmwT9u2Qxtj

tunnels:
  frontend:
    proto: http
    addr: 3000
    # Optional: Add a custom subdomain (requires paid plan)
    # subdomain: researchy-app

  backend:
    proto: http
    addr: 3001
    # Optional: Add a custom subdomain (requires paid plan)
    # subdomain: researchy-backend

  agent:
    proto: http
    addr: 8000
    # Optional: Add a custom subdomain (requires paid plan)
    # subdomain: researchy-agent
